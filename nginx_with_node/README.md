PARA RODAR A APP EXECUTE O COMANDO:
```
docker-compose up -d --build (pela primeira vez, ou quando fizer modificação)
docker-compose up -d (para as outras vezes)
```

PARA ACESSAR A APP USE O ENDEREÇO:
```
http://localhost:8080/
```